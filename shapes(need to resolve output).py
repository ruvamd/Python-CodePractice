from numpy.core.fromnumeric import shape

for (each shape){
    if (shape is ellipse){
        ellipses.add(shape)
    }
    if (shape is rectangle){
        rectangle.add(shape)
     }
    if (shape is triangle){
         triangle.add(shape)
     }
}